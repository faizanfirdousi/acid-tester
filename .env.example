# .env is OPTIONAL — the project works out of the box without it.
# These defaults already match docker-compose.yml.
# Only create this file if you want to connect to a different Postgres instance.

DB_HOST=localhost
DB_PORT=5432
DB_USER=acid_user
DB_PASSWORD=acid_pass
DB_NAME=acid_db
